# 변환 메모_260423_1256

## 생성 파일
- 가동대상인력_변환본_260423_1256.csv
- 인력운영현황_변환본_260423_1256.csv

## 변환 기준
1. **가동대상인력**
   - 원본: `[legacy] 2026년 PMO본부 KPI_가동율_260410.xlsx` 의 `인력재직현황`
   - PMO본부 재직 인력 17명을 추출했습니다.
   - `직급`은 원본 `직위`의 숫자 접두부를 제거하여 정규화했습니다.  
     예) `3이사 → 이사`, `4수석 → 수석`, `5책임 → 책임`, `6선임 → 선임`
   - `직무`는 원본/샘플 양식을 비교해 PM/PL로 정규화했습니다.
   - 월별 수치는 원본 시트의 값을 그대로 반영했고, `연간합계`는 1~12월 합계로 계산했습니다.

2. **인력운영현황**
   - 기본 프로젝트/인력 관계: `[legacy] PMO본부 업무수행 현황_260331.xlsx` 의 `업무수행현황`
   - 재직 인력 기준 정보: KPI 파일 `인력재직현황`
   - 일부 제안 프로젝트의 기간 보정 참고: KPI 파일 `인력_투입현황MM`
   - `구분`은 다음 기준으로 정규화했습니다.
     - 수행/수주/완료 프로젝트 → `이행`
     - 제안/LOSS/WIN/DROP/제안진행 프로젝트 → `제안`
     - 업무지원 프로젝트 → `지원`
   - `상태`는 다음 기준으로 정규화했습니다.
     - `수행중`, `제안진행` 등 진행형 상태 → `진행`
     - `완료`, `WIN`, `LOSS`, `DROP` 등 종료형 상태 → `완료`

3. **M/M 계산 방식**
   - 연도 기준: **2026년**
   - 월별 M/M은 **해당 월의 한국 영업일(주말/공휴일 제외)** 대비
     프로젝트 투입 영업일 비율로 계산했습니다.
   - 사용한 2026 공휴일 반영값에는 1/1, 2/16~2/18, 3/2, 5/1, 5/5, 5/25, 6/3, 8/17, 9/24~9/25, 10/5, 10/9, 12/25 등이 포함됩니다.
   - `총M/M` = 2026년 1~12월 월별 M/M 합계
   - `가득MM` = 동일 인력의 `이행` 행 `총M/M` 합계
   - `가동MM` = 동일 인력의 전체(`이행`+`제안`+`지원`) 행 `총M/M` 합계

## 확인이 필요한 원본 애매사항
- `[한전산업개발] 인터넷빌링시스템(도로조명시스템 포함) 유지보수 용역`
  - 원본 비고에 `26/01/15 + 26/07/15(6개월 연장)` 표기가 함께 있어,
    최종 변환본은 **연장 종료일(2026-07-15)** 기준으로 반영했습니다.
- 일부 ongoing 프로젝트는 KPI 상세투입 시트와 업무수행현황 시트의 기간이 다릅니다.
  - 이번 변환본은 **업무수행현황의 프로젝트 맥락**을 우선하고,
    제안성 단기 과업은 KPI 상세투입 시트도 함께 참고해 정리했습니다.

## 권장 검토 포인트
- 실제 프로그램 적재 전에 아래 항목만 한 번 확인하면 좋습니다.
  1. 인터넷빌링 유지보수의 2026년 상반기 PM 지원기간
  2. 제안진행 상태 프로젝트(P2026006~P2026011)의 종료 처리 기준
  3. `가득MM`에 `지원`을 포함할지 여부
